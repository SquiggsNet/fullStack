apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: user-server-ingress
  namespace: prod
spec:
  # tls:
  # - hosts:
  #   - app.squiggs.net
  #   secretName: squiggsnet-secret-tls
  backend:
    serviceName: user-server-service
    servicePort: user-server-prt